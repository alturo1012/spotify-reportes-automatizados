pandas>=2.0
openpyxl>=3.1
google-cloud-bigquery>=3.11   # si luego se conecta directo a BigQuery en vez del xlsx
python-dateutil>=2.8
spotipy>=2.23                 # API de Spotify, para la fecha de lanzamiento (ver src/spotify_release_dates.py)
python-dotenv>=1.0            # lee SPOTIFY_CLIENT_ID/SECRET de un archivo .env (ver .env.example)
